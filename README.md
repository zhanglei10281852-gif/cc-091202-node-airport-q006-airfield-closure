# 飞行区检查封闭资料服务

仓库提供跑道与滑行道区段拓扑、异物报告和叠加封闭措施样例。区段端点相同只表示相邻，限制是否重叠需要同时考虑区段集合与有效时间；现场记录均使用脱敏编号。

Node.js 20 以上版本执行 `npm test` 后可通过 `npm start` 启动，`GET /health` 返回健康状态。`docker compose up --build` 提供等价容器入口。机场生产地图、人员身份和访问凭据不能进入版本库。
