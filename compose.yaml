services:
  app:
    build: .
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      PORT: 3000
      EVENT_LOG: /data/events.jsonl
    volumes:
      - event-data:/data
    restart: unless-stopped

volumes:
  event-data:
